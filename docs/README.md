# @reactleaf/modal docs

This directory is the GitHub Pages surface for `@reactleaf/modal`, following the same high-level shape as `react-smooth-calendar/docs`.

It is intentionally lighter than the calendar docs:

- Vite instead of React Router/Fumadocs
- local source alias for `@reactleaf/modal`
- `GITHUB_PAGES_BASE` support for project Pages
